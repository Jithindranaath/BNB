# T-003 · External API probes

Date: 2026-09-07. Scripts: `scripts/probe/*.ts`. Raw output: `scripts/probe/output/`.
Rule R5: no typed client in `packages/data/sources/` for a response not printed here.

| Source | Status | Notes |
|---|---|---|
| Binance klines | **OK** | `GET /api/v3/klines?symbol=BNBUSDT&interval=1h&limit=5`. 12-col arrays: `[openTime,o,h,l,c,v,closeTime,quoteVol,trades,takerBuyBase,takerBuyQuote,ignore]`. No auth. |
| DefiLlama yields | **OK** | `GET /pools` → `{status,data:[…]}`, 17,280 pools. Per-pool keys incl. `pool,chain,project,symbol,tvlUsd,apy,apyBase,apyReward,underlyingTokens,…`. `GET /chart/{pool}` → daily `{timestamp,tvlUsd,apy,apyBase,apyReward}`, ~1,520 points for the sampled pool. No auth. |
| BSC RPC + Multicall3 | **OK** | PublicNode `https://bsc-rpc.publicnode.com`, no key. `viem` `multicall({multicallAddress: 0xcA11…})` → 4/4 `status:"success"`. This is the pattern `rpc.py` must use — never loop single calls. |
| Venus on-chain | **OK, with a caveat** | 55 Core-Pool markets; `vUSDT.underlying()` == USDT; `exchangeRateStored`, `supply/borrowRatePerBlock`, `totalBorrows`, `getCash`, `markets(vToken)` all read fine. **The Comptroller `0xfD36E2c2…` is an EIP-2535 Diamond** — `liquidationIncentiveMantissa()` reverts `Diamond: Function does not exist`. `getAllMarkets` / `closeFactorMantissa` / `oracle` do work. **T-044 action:** find the liquidation-incentive source (per-market view or a specific facet); do not assume the classic Comptroller ABI. |
| PancakeSwap v3 subgraph | **BLOCKED (still)** | A key was supplied 2026-09-07 but it was a **Studio deploy key**, not a query API key — the gateway returns `auth error: API key not found` for both candidate ids. The legacy hosted `api.thegraph.com/...` host is gone (`fetch failed`). **Need: a Subgraph Studio → API Keys → query key** (32 hex, used as `gateway.thegraph.com/api/<KEY>/subgraphs/id/<id>`). Then re-run `probe/pcs_subgraph.ts`; it tries both ids (`78EUqz…` "PancakeSwap V3 BSC", `Hv1Gnc…` "exchange-v3-bsc"), keeps the one returning a pool, and `verify_reference.ts` will then confirm `_meta.block.number` when the entry is flipped to `verified:true`. |
| BscScan API | **OK, with a caveat** | Key works via the **Etherscan API V2** multichain endpoint `api.etherscan.io/v2/api?chainid=56` (the old `api.bscscan.com` V1 host is fully deprecated → "switch to V2"). `getsourcecode` → OK (PancakeV3Factory, compiler `v0.7.6+commit.7338295f`, ABI 5.2 KB). **`getcontractcreation` → NOTOK on the free tier**: "Free API access is not supported for this chain." **T-031 impact:** the "contract age / creation block" check (spec §3.1) can't use BscScan on free — get it another way (Blockscout BSC, a provider enhanced-API, or `eth_getCode` bisection) and mark that check `UNVERIFIED` until wired. |

## Unblock steps

1. **`GRAPH_API_KEY`** — still needed. In [Subgraph Studio](https://thegraph.com/studio/) open the **API Keys** tab (NOT a subgraph's "deploy key") → create a key → put it in root `.env` as `GRAPH_API_KEY=`. Re-run `cd scripts && npx tsx probe/pcs_subgraph.ts`, then finalise `packages/data/reference/subgraphs.json` (`verified: true`, keep the winning id, record the loser) and re-run `npm run verify:reference`.
2. ~~`BSCSCAN_API_KEY`~~ — **done** (2026-09-07). In `.env`. `getsourcecode` works on free; `getcontractcreation` does not (see caveat above).

Status now: Binance / RPC / Venus / DefiLlama / **BscScan source** all usable →
T-010 `binance.py`, `rpc.py`, `venus.py`, `llama.py`, `bscscan.py` (source + verification only) are **unblocked**. Only `subgraph.py` still waits, on a real Graph query key.
